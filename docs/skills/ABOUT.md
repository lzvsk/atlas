---
mirrors:
  - plugins/atlas/skills/**
---

# skills/

The plugin's core skill. One folder per skill; the folder name is the skill's name.

## Folders

- `atlas/` — the `atlas` skill. Activates in atlas-style repos (`AGENTS.md` + `docs/` at the root). See `atlas/ABOUT.md` for details.
