---
mirrors:
  - hooks/**
---

# hooks/

Claude Code hooks provided by the plugin.

## Files

- `hooks.json` — hook declaration (which event → which command). See `hooks.md`.
- `remind-doc.sh` — executable hook script. See `remind-doc.md`.
